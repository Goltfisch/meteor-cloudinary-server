# goltfisch:cloudinary-server
This package adds the cloudinary NPM-Package to your Meteor project.

## Installation
Run `meteor add goltfisch:cloudinary-server` and you can use the `cloudinary` variable on the server-Side.
